<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register - Pixels Dojo</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .form-group { margin-bottom: 1.2rem; }
    label { display: block; margin-bottom: 0.4rem; color: #00ffff; font-weight: bold; }
    input, textarea { width: 100%; padding: 0.8rem; background: #111; border: 1px solid #00ffff33; border-radius: 6px; color: white; }
    .pfp-options { display: flex; flex-wrap: wrap; gap: 1rem; margin-top: 0.5rem; }
    .pfp-options label { cursor: pointer; text-align: center; }
    .pfp-options img { width: 60px; height: 60px; border-radius: 50%; border: 2px solid transparent; object-fit: cover; }
    .pfp-options input:checked + img { border-color: #00ffff; box-shadow: 0 0 10px #00ffff80; }
    .error { color: #ff5555; font-weight: bold; text-align: center; margin-bottom: 1rem; }
  </style>
</head>
<body>
  <div class="container" style="max-width:500px; margin:4rem auto; padding:2rem; background:rgba(0,0,0,0.6); border-radius:12px; border:1px solid #00ffff33;">
    <h1 style="text-align:center; color:#00ffff;">Register</h1>

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form method="POST" action="/register">
      <div class="form-group">
        <label>Username *</label>
        <input type="text" name="username" required placeholder="Choose a unique username">
      </div>

      <div class="form-group">
        <label>Email *</label>
        <input type="email" name="email" required placeholder="your@email.com">
      </div>

      <div class="form-group">
        <label>Password * (minimum 8 characters)</label>
        <input type="password" name="password" required minlength="8">
      </div>

      <div class="form-group">
        <label>Display Name (short public name) *</label>
        <input type="text" name="display_name" required placeholder="How you want to appear">
      </div>

      <div class="form-group">
        <label>Bio / Blurb (optional – short description about you)</label>
        <textarea name="bio" rows="4" placeholder="e.g. Pixel artist, coffee lover, crow tamer in training..."></textarea>
      </div>

      <div class="form-group">
        <label>Choose Profile Picture *</label>
        <div class="pfp-options">
          <% for (let i = 1; i <= 5; i++) { %>
            <label>
              <input type="radio" name="profile_image" value="/images/defaults/default<%= i %>.png" required <%= i === 1 ? 'checked' : '' %>>
              <img src="/images/defaults/default<%= i %>.png" alt="Default avatar <%= i %>">
            </label>
          <% } %>
        </div>
      </div>

      <button type="submit" style="width:100%; padding:1rem; background:#00cc00; color:white; border:none; border-radius:8px; font-weight:bold; cursor:pointer;">
        Create Account
      </button>
    </form>

    <p style="text-align:center; margin-top:1.5rem;">
      Already have an account? <a href="/login" style="color:#00ffff;">Login here</a>
    </p>
  </div>
</body>
</html>
