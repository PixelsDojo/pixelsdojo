<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
<div style="margin-bottom:2rem;">
  <a href="/" style="color:#00ffff; text-decoration:none; font-weight:bold;">← Back to Home</a>
</div>
  <title>My Profile - Pixels Dojo</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .profile-container {
      max-width: 700px;
      margin: 4rem auto;
      padding: 2.5rem;
      background: #1a1a2e;
      border-radius: 16px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.6);
      color: white;
      text-align: center;
    }
    .profile-avatar {
      width: 160px;
      height: 160px;
      border-radius: 50%;
      object-fit: cover;
      border: 5px solid #00ffff;
      margin-bottom: 1.5rem;
    }
    .profile-name {
      font-size: 2.8rem;
      margin: 0.5rem 0;
      color: #00ffff;
    }
    .profile-username {
      font-size: 1.4rem;
      color: #aaa;
      margin-bottom: 1.5rem;
    }
    .profile-info {
      background: rgba(255,255,255,0.06);
      padding: 2rem;
      border-radius: 12px;
      margin: 2rem 0;
      text-align: left;
    }
  </style>
</head>
<body>
<header>
  <div class="header-container">
    <div class="logo-section">
      <img src="/images/pixeldojo_logo_round_.png" alt="Pixels Dojo" class="logo">
      <div class="logo-text">
        <h1>PIXELS DOJO</h1>
        <p class="tagline">Master TerraVilla • Earn $PIXEL • Join the Community</p>
      </div>
    </div>

    <div class="right-section">
      <div class="user-nav">
        <% if (user) { %>
          <span>Welcome, <%= user.display_name || user.username %>!</span>
          <% if (user.is_admin) { %>
            <a href="/admin">Admin</a>
          <% } %>
          <a href="/logout">Logout</a>
        <% } else { %>
          <a href="/login">Login</a> |
          <a href="/register">Register</a>
        <% } %>
      </div>

      <div class="price-widget">
        <script src="https://widgets.coingecko.com/coingecko-coin-price-marquee-widget.js"></script>
        <coingecko-coin-price-marquee-widget 
          coin-ids="pixels" 
          currency="usd" 
          background-color="transparent" 
          font-color="#edff84">
        </coingecko-coin-price-marquee-widget>
      </div>
    </div>
  </div>
</header>

 <nav>
        <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="/npcs">NPCs</a></li>
            <li><a href="#new">I'm New</a></li>
            <li><a href="#earn">Earn $PIXEL</a></li>
            <li><a href="#master">Become Master</a></li>
            <li><a href="#community">Community</a></li>
        </ul>
    </nav>

  <div class="profile-container">
    <img 
      src="<%= user && user.profile_image ? user.profile_image : '/images/default-avatar.png' %>" 
      alt="Profile picture" 
      class="profile-avatar"
      onerror="this.src='/images/default-avatar.png'"
    >

    <h1 class="profile-name"><%= user && user.display_name ? user.display_name : (user && user.username ? user.username : 'Guest') %></h1>
    <p class="profile-username">@<%= user && user.username ? user.username : 'not logged in' %></p>

    <div class="profile-info">
      <h3>About Me</h3>
      <p><%= user && user.bio ? user.bio : 'No bio yet — edit your profile to add one!' %></p>
      <p><strong>Joined:</strong> <%= user && user.created_at ? new Date(user.created_at).toLocaleDateString('en-US', { month: 'long', year: 'numeric' }) : 'Unknown' %></p>
    </div>

    <a href="/profile/edit" class="btn" style="display:inline-block;margin-top:2rem;padding:0.9rem 2rem;background:#6a0dad;color:white;border-radius:8px;text-decoration:none;">Edit Profile</a>
  </div>
</body>
</html>
