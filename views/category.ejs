<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= category.charAt(0).toUpperCase() + category.slice(1) %> Guides - Pixels Dojo</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('partials/header') %>

  <div class="container" style="max-width: 1300px; margin: 3rem auto; padding: 0 1.5rem;">
    <!-- Big category title with glow -->
    <h1 style="color: var(--yellow); text-shadow: 0 0 12px rgba(237, 255, 132, 0.5); text-align: center; margin-bottom: 3rem;">
      <%= category.charAt(0).toUpperCase() + category.slice(1) %> Guides
    </h1>

    <% if (pages && pages.length > 0) { %>
      <!-- Card grid – uses your existing .card-grid and .card classes -->
      <div class="card-grid">
        <% pages.forEach(page => { %>
          <div class="card">
            <% if (page.screenshots && page.screenshots.length > 0 && page.screenshots[0]) { %>
  <img 
    src="<%= page.screenshots[0] %>" 
    alt="<%= page.title %>" 
    style="width: 100%; height: 180px; object-fit: cover; border-bottom: 3px solid var(--cyan);"
    onerror="this.src='/images/placeholder-category.jpg'" <!-- fallback if image fails -->
  >
<% } else { %>
  <div style="height: 180px; background: rgba(0, 212, 255, 0.1); display: flex; align-items: center; justify-content: center; font-size: 2rem; color: var(--cyan); border-bottom: 3px solid var(--cyan);">
    No Image Yet
  </div>
<% } %>

            <div style="padding: 1.5rem;">
              <h3 style="color: var(--cyan); margin-bottom: 0.8rem;">
                <a href="/pages/<%= page.slug %>" style="color: inherit; text-decoration: none;">
                  <%= page.title %>
                </a>
              </h3>
              <p style="color: #b8b8b8; font-size: 1rem; margin-bottom: 1rem;">
                <%= page.summary || (page.content.substring(0, 150) + '...') %>
              </p>
              <small style="color: var(--cyan-light);">
                By <%= page.author_display_name || 'Community' %> • 
                <%= new Date(page.created_at).toLocaleDateString() %>
              </small>
            </div>
          </div>
        <% }) %>
      </div>

      <!-- Pagination or "View more" if you add it later -->
      <div style="text-align: center; margin-top: 3rem;">
        <a href="/" class="btn btn-cyan">Back to Home</a>
      </div>
    <% } else { %>
      <!-- Empty state – styled like wiki cards -->
      <div style="text-align: center; padding: 4rem; background: rgba(20, 20, 30, 0.7); border-radius: 12px; border: 2px solid var(--purple); box-shadow: 0 8px 30px rgba(133, 0, 255, 0.25);">
        <h3 style="color: var(--yellow); margin-bottom: 1rem;">No guides in this category yet</h3>
        <p style="color: #b8b8b8; margin-bottom: 2rem;">Be the first to add some amazing content!</p>
        <% if (user && user.is_admin) { %>
          <a href="/admin" class="btn btn-primary">Add a Guide Now →</a>
        <% } %>
      </div>
    <% } %>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
