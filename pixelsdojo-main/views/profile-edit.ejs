<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profile | Pixels Dojo</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/chat-widget.css">
  <style>
    .profile-edit-container {
      max-width: 900px;
      margin: 2rem auto;
      padding: 2rem;
      background: rgba(20, 20, 40, 0.8);
      border-radius: 12px;
      border: 1px solid rgba(0, 255, 255, 0.2);
      backdrop-filter: blur(6px);
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      color: var(--cyan, #00ffff);
      font-weight: 500;
    }

    .form-group input[type="text"],
    .form-group input[type="email"],
    .form-group textarea,
    .form-group input[type="file"] {
      width: 100%;
      padding: 0.8rem;
      background: rgba(0, 0, 0, 0.4);
      border: 1px solid rgba(0, 255, 255, 0.3);
      border-radius: 6px;
      color: white;
      font-size: 1rem;
    }

    .form-group input:disabled,
    .form-group textarea:disabled {
      background: rgba(50, 50, 70, 0.6);
      color: #aaa;
      cursor: not-allowed;
    }

    .form-group input:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: var(--cyan);
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
    }

    .form-group small {
      color: #aaa;
      font-size: 0.85rem;
      display: block;
      margin-top: 0.3rem;
    }

    .current-avatar {
      margin-top: 0.5rem;
      border-radius: 50%;
      border: 2px solid var(--cyan);
      max-width: 120px;
    }

    .btn-save {
      background: var(--cyan);
      color: black;
      padding: 0.8rem 1.5rem;
      border: none;
      border-radius: 8px;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.2s;
    }

    .btn-save:hover {
      background: var(--yellow);
      transform: translateY(-2px);
    }

    .error { color: #ff4444; margin-bottom: 1rem; }
    .success { color: #65eb05; margin-bottom: 1rem; }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <nav>
    <ul>
      <li><a href="/">Home</a></li>
      <li><a href="/category/startup">Start Here</a></li>
      <li><a href="/category/earn">Earn $PIXEL</a></li>
      <li><a href="/category/mastery">Mastery</a></li>
      <li><a href="/npcs-map">NPCs & Map</a></li>
        <li><a href="/events">Events</a></li>
        <li><a href="/amas">AMAs</a></li>
      <li><a href="/category/community">Community</a></li>
    </ul>
  </nav>

  <div class="main-wrapper">
    <!-- Sidebar – same as other pages -->
    <aside class="sidebar">
      <h4>Library</h4>
      <a href="/">🏠 All Articles</a>
      <a href="/npcs-map">🗺️ NPCs & Map</a>
      <hr>
      <h5>Categories</h5>
      <a href="/category/startup">Start Here</a>
      <a href="/category/earn">Earn $PIXEL</a>
      <a href="/category/mastery">Mastery</a>
      <a href="/category/community">Community</a>
    </aside>

    <div class="main-content-area">
      <div class="profile-edit-container">
        <h1>Edit Profile</h1>

        <% if (locals.error) { %>
          <p class="error"><%= error %></p>
        <% } %>

        <% if (locals.success) { %>
          <p class="success">Profile updated successfully!</p>
        <% } %>

        <form action="/profile/edit" method="POST" enctype="multipart/form-data">
          <!-- Profile Picture – EVERYONE can edit -->
          <div class="form-group">
            <label for="profile_image">Profile Picture</label>
            <input type="file" id="profile_image" name="profile_image" accept="image/*">
            <% if (user.profile_image) { %>
              <img src="<%= user.profile_image %>" alt="Current avatar" class="current-avatar">
            <% } %>
          </div>

          <!-- Display Name – EVERYONE can edit -->
          <div class="form-group">
            <label for="display_name">Display Name *</label>
            <input type="text" id="display_name" name="display_name" 
                   value="<%= user.display_name || user.username %>" required>
          </div>

          <!-- Username – EVERYONE can edit -->
          <div class="form-group">
            <label for="username">Username *</label>
            <input type="text" id="username" name="username" 
                   value="<%= user.username %>" required>
            <small style="color: #aaa;">This is your login name</small>
          </div>

          <!-- Email – EVERYONE can edit -->
          <div class="form-group">
            <label for="email">Email *</label>
            <input type="email" id="email" name="email" 
                   value="<%= user.email %>" required>
          </div>

          <!-- Password Change (optional) – EVERYONE -->
          <div class="form-group">
            <label for="new_password">New Password (leave blank to keep current)</label>
            <input type="password" id="new_password" name="new_password" minlength="8" 
                   placeholder="Enter new password or leave blank">
          </div>

          <hr style="border-color: rgba(0,255,255,0.2); margin: 2rem 0;">

          <!-- Bio – CONTRIBUTORS AND ADMINS ONLY -->
          <div class="form-group">
            <label for="bio">Bio / About You</label>
            <textarea id="bio" name="bio" rows="5" 
                      <%= (user.is_admin || user.is_contributor) ? '' : 'disabled' %>>
<%= user.bio || '' %>
            </textarea>
            <% if (!(user.is_admin || user.is_contributor)) { %>
              <small>(Contributors and admins only - Want to become a contributor? Contact admin!)</small>
            <% } %>
          </div>

          <!-- Social Links – CONTRIBUTORS AND ADMINS ONLY -->
          <div class="form-group">
            <label for="social_links">Social Links (one per line: Platform | URL)</label>
            <textarea id="social_links" name="social_links" rows="4" 
                      <%= (user.is_admin || user.is_contributor) ? '' : 'disabled' %>>
<%= user.social_links ? user.social_links.split(',').join('\n') : '' %>
            </textarea>
            <% if (!(user.is_admin || user.is_contributor)) { %>
              <small>(Contributors and admins only)</small>
            <% } %>
          </div>

          <!-- Tip Address – CONTRIBUTORS AND ADMINS ONLY -->
          <div class="form-group">
            <label for="tip_address">Tip / Donation Address (PayPal, wallet, etc.)</label>
            <input type="text" id="tip_address" name="tip_address" 
                   value="<%= user.tip_address || '' %>" 
                   <%= (user.is_admin || user.is_contributor) ? '' : 'disabled' %>>
            <% if (!(user.is_admin || user.is_contributor)) { %>
              <small>(Contributors and admins only)</small>
            <% } %>
          </div>

          <button type="submit" class="btn-save">Save Changes</button>
          
          <hr style="border-color: rgba(0,255,255,0.2); margin: 2rem 0;">
          
          <!-- Delete Account – EVERYONE -->
          <div style="text-align: center; margin-top: 2rem;">
            <button type="button" onclick="deleteAccount()" 
                    style="background: #ff4444; color: white; padding: 0.8rem 1.5rem; border: none; border-radius: 8px; font-weight: bold; cursor: pointer;">
              Delete My Account
            </button>
            <p style="color: #999; font-size: 0.9rem; margin-top: 0.5rem;">
              Warning: This action cannot be undone!
            </p>
          </div>
        </form>

        <script>
          function deleteAccount() {
            if (confirm('Are you ABSOLUTELY SURE you want to delete your account?\n\nThis will:\n- Delete all your data\n- Remove all your posts\n- Cannot be undone\n\nType DELETE in the next prompt to confirm.')) {
              const confirmation = prompt('Type DELETE to confirm account deletion:');
              if (confirmation === 'DELETE') {
                fetch('/profile/delete', {
                  method: 'POST',
                  headers: { 'Content-Type': 'application/json' }
                })
                .then(res => res.json())
                .then(data => {
                  if (data.success) {
                    alert('Your account has been deleted.');
                    window.location.href = '/';
                  } else {
                    alert('Error: ' + data.error);
                  }
                })
                .catch(err => {
                  alert('Failed to delete account. Please try again.');
                });
              }
            }
          }
        </script>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
